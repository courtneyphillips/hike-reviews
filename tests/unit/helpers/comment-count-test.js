import { commentCount } from '../../../helpers/comment-count';
import { module, test } from 'qunit';

module('Unit | Helper | comment count');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = commentCount(42);
  assert.ok(result);
});
